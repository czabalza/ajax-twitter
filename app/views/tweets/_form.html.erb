<h2>New Tweet!</h2>
<form class="tweet-compose" data-ul-id="feed">
  <strong class="chars-left">140</strong>
  <label>
    Content
    <textarea name="tweet[content]"></textarea>
  </label>
  <br>

  <label>
    Mention
    <script type="text/template" id="mention-script">
      <select name="tweet[mentioned_user_ids][]">
        <option></option>
        <% User.all.each do |user| %>
          <option value="<%= user.id %>"><%= user.username %></option>
        <% end %>
      </select>
    </script>
    <a href="javascript:void(0)" class="add-mentioned-user">Add Mention</a>

    <div class="mentioned-users"></div>

  </label>
  <br>

  <input type="Submit" value="Post Tweet!">
</form>
